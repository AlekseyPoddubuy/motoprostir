<% if admin_signed_in? %>
sssssssssss
<div class="container">
	<div class="row">
		<div class="col-lg-12">

			<%= simple_form_for(@review) do |f| %>
			<%= f.error_notification %>

			<div class="form-inputs">
				<%= f.input :name %>
				<%= f.input :surname %>
				<%= f.input :body %>
				<%= f.input :status %>
			</div>

			<div class="form-actions">
				<%= f.button :submit %>
			</div>
			<% end %>

		</div>
	</div>
</div>
<% end %>