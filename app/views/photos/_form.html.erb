<div class="container">
	<div class="row">
		<div class="col-lg-12">
			<%= form_for([@item,@item.photos.build]) do |f| %>
			<%= f.file_field :avatar %>
			<%= f.submit %>
			<% end %>
		</div>
	</div>
</div>